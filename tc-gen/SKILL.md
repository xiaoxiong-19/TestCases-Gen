---
name: tc-gen
description: 基于版本迭代需求文档，结合本地 .dev-standards 代码知识库、业务文档、代码地图、流程定义和三方接口文档，分阶段生成有证据链的测试概要与测试用例；MCP 可选增强。阶段4完成后须询问是否转为 XMind，用户同意后再转换。用于用户说 tc-gen、初始化版本、生成测试概要、生成测试用例、转 XMind、md to xmind、做变更影响分析或精准回归时。
---

# tc-gen：新需求测试用例生成

为项目新需求生成高质量测试概要与测试用例。**分 5 个阶段，每个阶段产出后必须停下来等用户确认/纠正，禁止一次性走完。**

本技能以**本地需求文档 + 本地代码知识库（`.dev-standards`）**为主；`user-dev-standards` MCP 为**可选增强**（可用于知识发现、需求校验和交叉验证），**不强制调用**。若 MCP 不可用、调不通、超时或无匹配结果：**记录一次原因后立即 fail-open 回退本地流程，禁止重试/等待/卡死**；若未使用 MCP，也无需强行补调。

## 核心原则

- **需求文档只写「增量」，代码知识库（`.dev-standards`）写「全量」。** 有知识库时必须结合两者：既测新功能，又不漏被影响的老功能。
- **本地知识库优先，MCP 可选。** 不强制读取/调用 MCP。若 MCP 可用且用户环境已配置，可选用它辅助定位业务规则、需求疑点和服务知识；最终仍以本地业务文档、代码地图、流程定义和接口文档全文为准。
- **接地气且可追溯。** 所有测试点必须有证据链：需求变更项、业务文档章节、状态机/配置矩阵/通知矩阵、接口字段、代码地图 Handler/风险点；若使用了 MCP，可补充 MCP 业务规则来源。禁止编造泛泛用例。
- **人工卡点。** 每阶段结束输出「请确认是否继续」，等用户回复后再进入下一阶段。
- **版本隔离。** 每个迭代版本独立放在 `.test-standards/<版本>/` 下；`.test-standards` 根目录只初始化一次，不在根目录直接放 `input/output`。
- **严禁以活源码替代知识库。** `codemap`/业务文档/`procdefs` 均指代码知识库（默认路径 `.dev-standards`）下预先沉淀的**静态文档**，不是项目活源码。测试用例的依据是「系统应该做什么」（需求文档 + 接口规范），不是「系统现在写了什么」（活源码）。原因：① 同步开发的新功能代码在开发人员本机，远程仓库尚未有最新提交，读到的是不完整的早期骨架；② 用代码反推测试用例本质上是「以代码校验代码」，无法发现实现偏差。

## 调用方式与初始化规则

当用户说类似 `tc-gen初始化版本“V1.12.0-xxx”`、`tc-gen 初始化版本 V1.12.0-xxx` 时，执行初始化，不进入阶段0。

### 脚本路径约定

- **不要写死** `.cursor/skills/...` 或其他 IDE 私有路径。
- `tc-gen` 内置脚本路径一律相对于**本 skill 安装目录**（即包含本 `SKILL.md` 的目录），记作 `<本skill目录>`。
  - 示例：若技能装在当前软件 skills 下的 `tc-gen/`，则执行
    `python "<本skill目录>/scripts/init_version_utf8.py" ...`
  - Agent 运行前须把 `<本skill目录>` 解析为加载本 skill 时的真实绝对路径。
- Word/Excel → md、测试用例 md → XMind 均在本 skill 的 `scripts/` 下，**不再依赖独立的 `tc-convert` skill**。
- 下文命令中的 `"<本skill目录>/scripts/..."` 仅为占位符。

初始化规则：

1. 在代码项目根目录下检查 `.test-standards`。
2. 如果 `.test-standards` 不存在，则创建；如果已存在，不重复初始化根目录，不覆盖其中已有文件。
3. 初始化版本目录时，必须使用本 skill 内置 UTF-8 脚本，禁止用 PowerShell/cmd 直接 `mkdir` 或 `New-Item` 创建含中文版本目录：

```bash
python "<本skill目录>/scripts/init_version_utf8.py" "<项目根目录>" "<版本>"
```

4. 如果 `<版本>` 包含中文、空格或 shell 中显示乱码，且版本目录**尚不存在**：用 Python 把版本名写到系统临时目录（`tempfile.gettempdir()`）UTF-8 文本文件首行，再使用 `--version-file`，避免 Windows GBK/CP936 终端把中文参数解码错误。**禁止**把该文件写进项目或 `.test-standards/`。

```bash
python "<本skill目录>/scripts/init_version_utf8.py" "<项目根目录>" --version-file "<UTF-8版本名文件>"
```

若版本目录已存在，含中文时优先 `--version-prefix`（ASCII 前缀能唯一匹配即可），不必再写临时文件。

5. 脚本会在 `.test-standards` 下创建版本目录：`.test-standards/<版本>/`。
6. 在版本目录下创建：

```text
.test-standards/<版本>/
├── input/
│   ├── prodword/      # 需求文档目录，沿用原 requirement 逻辑
│   │   └── prodword_pic/  # 需求文档图片目录，Word 转 md 时同步抽取图片
│   └── reference/     # 三方接口文档/联动参考资料，沿用原 reference 逻辑
└── output/            # 阶段产物目录，沿用原 output 逻辑
```

7. 如果版本目录已存在，只补齐缺失子目录，不删除、不覆盖已有输入或输出。
8. 初始化完成后，提示用户把需求文档放到 `input/prodword/`，把接口/三方/联动资料放到 `input/reference/`。`input/prodword/prodword_pic/` 由转换流程存放需求文档图片，用户也可手动补充流程图原图。
9. `.test-standards` 根目录下不得直接创建版本无关的 `input/` 或 `output/`。

### 版本名参数约定（初始化 / 阶段0 / 转 XMind 通用）

- 所有版本目录操作必须走本 skill 的 UTF-8 脚本（`init_version_utf8.py` / `list_version_inputs_utf8.py` / `convert_version_inputs_utf8.py` / `convert_cases_to_xmind_utf8.py`）。
- **目录已存在**（阶段0 发现输入、Word 转换、转 XMind、补齐缺失子目录）：含中文时优先 `--version-prefix`（ASCII 前缀能唯一匹配即可）。不要把版本名临时文件写进项目或 `.test-standards/`。
- **目录尚不存在**（首次初始化且版本名含中文）：用 Python 把版本名写到 `tempfile.gettempdir()` 下 UTF-8（或 UTF-8 with BOM）文本文件首行，再 `--version-file`。**禁止**写进项目或 `.test-standards/`。
- 版本名为纯 ASCII 时可直接传 `"<版本>"` 位置参数。
- 初始化脚本只创建目录，不覆盖已有输入文件和阶段产物。
- Agent 不得用 shell 内嵌中文命令创建版本目录，例如 `mkdir ".test-standards/V1.0.0-支付宝"`、`New-Item`、`python -c "..."`。

### 版本输入发现要求

- 阶段0开始前，必须先运行本 skill 内置 UTF-8 脚本发现版本目录和输入文件，禁止用 `Glob` 直接扫描 `.test-standards/<中文版本>/`：

```bash
python "<本skill目录>/scripts/list_version_inputs_utf8.py" "<项目根目录>" --version-prefix "V1.0.0-saas"
```

- 脚本输出 JSON，包含 `versionRoot`、`input/prodword`、`prodword_pic`、`input/reference`、`output` 的存在性和文件清单。后续读取、转换、图片解析必须以该 JSON 中的绝对路径为准。
- 若 `Glob` 返回 0 个文件但脚本能找到文件，以脚本结果为准；不得据此判断用户未初始化或未放文件。

### 阶段0 Word/Excel 转换要求（禁止自写脚本）

阶段0 发现 `.docx` / `.xlsx` 后，**必须**调用本 skill 内置脚本转换，禁止 Agent 现场编写转换脚本：

```bash
python "<本skill目录>/scripts/convert_version_inputs_utf8.py" "<项目根目录>" --version-prefix "V1.6.1"
```

- 该脚本会转换 `input/prodword/` 与 `input/reference/` 下的 `.docx` / `.xlsx` 为同名 `.md`（保留原文件），并把 Word 图片抽到 `input/prodword/prodword_pic/`。
- 底层转换实现是同目录 `scripts/convert_to_md.py`（保留表格为 Markdown 表）。Agent 不要绕过 wrapper 自己拼 pandoc / mammoth / `python -c`。
- **禁止**：在项目或 `.test-standards/` 下新建 `_convert_*.py`、`_version_name_utf8.txt` 等临时文件；禁止 `python -c` 内嵌 mammoth/python-docx；禁止因 pandoc 不在 PATH 就手写转换器。
- 原因：mammoth 会把 Word 表格展成段落，配置/字段表丢失结构；现场脚本还容易把临时文件留在需求目录。
- `.doc` / `.xls`（旧格式）本脚本不转：在阶段0摘要提示用户另存为 `.docx` / `.xlsx` 后重跑，不要硬猜内容。
- 转换细节与自检见 `reference.md` § 一。

## 代码知识库路径规则

默认代码知识库根路径为：

```text
.dev-standards/
```

常见子目录（存在则读取，不存在不强求齐全）：

```text
.dev-standards/knowledge/business/   # 业务规则/业务流程
.dev-standards/knowledge/codemap/    # 代码地图
.dev-standards/knowledge/service/    # 服务知识
.dev-standards/procdefs/             # 流程定义
```

定位顺序：

1. 先在当前 workspace / 项目根及其常见兄弟模块下查找 `.dev-standards/`（例如项目根、`*-common/.dev-standards/`）。
2. 若用户在本轮对话中已明确提供知识库路径，直接使用用户指定路径，不再重复询问。
3. 若默认路径不存在或为空：**立即停止定位读取**，明确提示用户：

> 当前项目未找到代码知识库（默认路径 `.dev-standards`）。  
> 是否有其他知识库目录？若有，请直接告诉 AI 知识库绝对/相对路径；若无，请确认是否仅基于需求文档与 `input/reference/` 接口文档继续。

4. 用户提供替代路径后，将该路径视为本次（及后续同版本）只读知识库根目录，并在阶段产物中写明实际使用路径。
5. 用户确认无知识库、仅基于需求/接口继续时，后续阶段按知识库缺失降级处理，并在产物中标注覆盖不完整风险。

## MCP 使用规则（可选，不强制；调不通必须 fail-open）

- **不强制要求读取/调用 MCP。** 无 MCP、MCP 不可用、调不通、超时、无工具、参数错误、或不想使用 MCP 时，可完全基于本地需求文档 + 代码知识库推进。
- **Fail-open 铁律（防止卡死，优先级高于一切 MCP 相关步骤）：**
  1. MCP 不是流程前置条件；任何阶段都不得因为“还没调通 MCP”而暂停、等待、轮询或反复重试。
  2. 同一会话内，每个 MCP 工具**最多尝试 1 次**。出现不可用/超时/报错/无返回/工具不存在/descriptor 读不到时：**立即停止该 MCP 调用**，记录原因，**马上回退本地文档继续**。
  3. **禁止**：同一工具连调多次、改参死磕、等待用户先修好 MCP、因 MCP 失败终止阶段0~4、把 MCP 失败写成阻断项。
  4. 调用 MCP 前若发现工具列表中无 `user-dev-standards` 相关工具，**直接跳过**，不要尝试调用。
  5. 读取 MCP descriptor/schema 失败时，视为 MCP 不可用，**直接跳过**，不要卡在“先读 schema”。
  6. MCP 成功与否都不影响进入下一动作：阶段0可做本地需求自检；阶段1以 `.dev-standards`/用户指定知识库为准；阶段2/3以需求+本地知识库+接口文档为准。
- 若环境中存在 `user-dev-standards` MCP **且**决定使用，调用前可读对应工具 descriptor/schema；读不到或调不通则按上面 fail-open 处理。
- 常用 MCP 工具（可选，全部可跳过）：
  - `get-business-rule`：阶段1辅助查询候选业务规则文档。建议先列表，再按关键词匹配。`projectPath` 禁止写死固定盘符路径，默认从当前 workspace 根目录自动推断。
  - `validate-requirement`：阶段0/阶段2前可选校验需求文档质量。
  - `get-service-knowledge`：阶段2/阶段3可选查询服务知识。
  - `query-standard`：仅在涉及代码评审、异常处理、事务、日志、数据库等规范风险时可选使用。
- 若本阶段**使用了 MCP**、尝试失败或主动跳过，建议在产物中写明；**未使用/调不通 MCP 都不是阻断项**：

```markdown
## MCP使用情况

- 未调用/失败/跳过原因：例如 MCP 不可用、调不通、超时、无匹配工具、本技能不强制 MCP，已 fail-open 回退本地需求文档与代码知识库。
- 处理：未重试、未等待修复；已继续当前阶段本地流程。
- 可选风险提示：未经过 MCP 交叉校验时，可能遗漏某些未写入本地知识库的补充规则。
```

## 输入输出约定

- 版本根目录：`.test-standards/<版本>/`，例如 `.test-standards/V1.12.0-xxx/`。
- 输入：`.test-standards/<版本>/input/prodword/`（需求文档，沿用原 requirement 逻辑）、`.test-standards/<版本>/input/prodword/prodword_pic/`（需求文档图片，尤其流程图）、`.test-standards/<版本>/input/reference/`（三方接口文档等联动文档）。
- 输出：`.test-standards/<版本>/output/` 下，按阶段写文件：`01-定位.md`、`02-变更分析.md`、`03-测试概要.md`、`04-测试用例.md`。
- 本地代码知识库（只读，默认）：`.dev-standards/knowledge/business/`、`.dev-standards/knowledge/codemap/`、`.dev-standards/knowledge/service/`、`.dev-standards/procdefs/`；若用户另行指定知识库根路径，则在该根路径下按同样相对结构查找。
- MCP知识源（可选）：`user-dev-standards` 下的业务规则、服务知识、需求校验、代码规范工具。
- 用例格式使用本技能内置的阶段4模板，固定字段为：`用例等级`、`所属模块`、`用例标题`、`前置条件`、`用例步骤`、`预期结果`。其中 `前置条件` 非必填；执行 `tc-gen` 时不需要额外读取 `.test-standards/templates/测试用例模板.md`。
- 各阶段细节参照同目录 `reference.md`：§ 一 Word/Excel 转换、§ 二 状态迁移、§ 三 接口补充清单、§ 五 操作偏离提问、§ 六 回归分析、§ 七 XMind（须用户同意）。裂变规则以本文件阶段4为准（`reference.md` § 四仅作指针）。

## 阶段进度清单

```markdown
- [ ] 阶段0 准备：Word 转 md + 读取输入 +（可选）需求质量自检/MCP校验
- [ ] 阶段1 定位：解析代码知识库路径 + 读取业务文档/codemap/procdefs（MCP 可选）
- [ ] 阶段2 变更分析：需求 vs 业务现状（服务知识/MCP 可选）
- [ ] 阶段3 测试概要：通过前置门禁后按6维度列测试点
- [ ] 阶段4 详细用例 + 回归清单
```

## 阶段0：准备（读取输入 + 可选需求校验）

1. 明确当前处理的 `<版本>`；若用户未给出版本，先询问，不要把产物写到 `.test-standards` 根目录。
2. 使用 `tc-gen/scripts/list_version_inputs_utf8.py` 列出 `.test-standards/<版本>/input/prodword/`、`.test-standards/<版本>/input/prodword/prodword_pic/` 和 `.test-standards/<版本>/input/reference/` 下的所有文件；Windows/中文路径场景禁止用 `Glob` 的空结果作为缺文件结论。
3. 对 `input/prodword/` 与 `input/reference/` 下每个 `.docx` / `.xlsx` 转为同名 `.md`（保留原文件），Word 图片抽到 `input/prodword/prodword_pic/`。**必须**运行 `convert_version_inputs_utf8.py`（见上文「阶段0 Word/Excel 转换要求」），禁止自写转换脚本。转换后的 md 中图片引用应指向 `prodword_pic/`；无法自动建立引用时，在 md 附近标注 `[图片：prodword_pic/<文件名>，需人工/AI查看]`。表格必须仍是 Markdown 表（配置/字段表丢了要重跑脚本，不要改用 mammoth）。细节见 `reference.md` § 一。
4. **正文 + 图片一起构成需求；禁止「只读字、假装图不存在」，也禁止「按目录读光所有图」。**  
   正确姿势：**先读全文建立骨架与缺口清单 → 再按缺口/优先级视觉读必要图片 → 每张读完立刻沉淀为可测试文字**。后续阶段以「正文 + 已沉淀的读图要点」为准，不依赖会话里留着原始像素。

5. **第一步：只读文本，建两张表（此步禁止视觉 Read 图片）。**
   1. 完整读取需求 md + `input/reference/` 文本（xlsx 已转 md 的读 md）。接口/模板类留意：字段、必填、枚举、校验、异常码、超时/重试/幂等。
   2. **图片引用索引表**：每张被 md 引用的图 → 路径、邻近标题/段落、候选级别（见下节 P0/P1/P2/P3）。
   3. **正文缺口清单**（决定哪些图非读不可），例如：
      - 正文写「如图」「见下图」「页面示意」「红框」「仅界面可见」但未列出字段/按钮/分支；
      - 规则只出现在截图里的控件名、必填星号、tab、状态标签、校验文案；
      - 流程/时序/状态只在图中，正文无步骤列表；
      - 同功能多张连图时，**先标缺口**，再选代表图，不要无缺口也按 image 序号全读。
   4. `list_version_inputs_utf8.py` 清单只做路径对齐；**禁止**无索引、无缺口地按文件名批量 Read。

6. **第二步：按优先级视觉读图（有些内容必须看图——在这里保证）。**  
   对索引中的每张图先定级，**只读 P0/P1（及预算内的 P2）**：

   | 级别 | 含义 | 是否必须视觉 Read | 典型 |
   |---|---|---|---|
   | **P0** | 流程/状态/**结构**专图，或正文明确以该图为准 | **必须读**（预算内优先占满） | 标注了流程图/时序图/泳道图/状态图；「以图为准」的总览 |
   | **P1** | 正文有缺口，验收点（字段/按钮/分支/校验/步骤）离开该图写不清 | **必须读** | 「新增模版」弹窗字段只在图上；导入步骤结果区无文字说明 |
   | **P2** | 正文已写清主规则，图仅补充布局/示意 | **选读**（预算剩余才读；同功能多屏只抽 1 张代表） | 与文字重复的列表页、与上一张几乎相同的渠道换皮页 |
   | **P3** | 图标、装饰、未引用、纯重复 | **不读** | logo、空白示意、与 P1 重复的第二张同屏 |

   - **P0 关键字（专图）**：`流程图`、`业务流程图`、`时序图`、`泳道图`、`状态流转`、`状态图`、`状态机`。不要仅因「步骤/模版/开证/页面」就升为 P0。
   - **P1 判定（UI 也要读）**：邻近正文无法独立写出可断言的测试点（缺控件名、缺必填、缺分支、缺结果反馈）→ 升 P1，**必须看图补全**，不能用「UI 默认不读」跳过。
   - **同功能多图**：同一能力（如「批量导入」）连续多张向导截图 → 优先读**结果变化最大**的 1～2 张（入口+校验结果 / 成功失败），其余 P2/P3，并在清单写「与 imageX 同功能，规则已由正文+imageX 覆盖」。
   - 每读完一张：立刻输出用户可见要点（控件/规则/分支/与正文差异）；图文冲突列疑点。要点必须写成**可进测试点的句子**，不要只写「见图」。

7. **第三步：视觉读图硬闸（防止卡死；限制的是节奏与总量，不是禁止参考图片）。**
   1. **同一轮工具调用最多视觉 Read 1 张图**（禁止并行多张 png）。
   2. **读完 1 张 → 必须先打出该图要点 → 再读下一张**；禁止 silently 连读。
   3. **阶段0 默认视觉读上限 8 张**（P0+P1 优先）。  
      - P0+P1 合计 ≤8：应读完所有 P0/P1。  
      - P0+P1 合计 >8：先读完所有 P0，再按「缺口对主链路影响」排序读 P1，直到 8 张；**未读到的 P1 必须在阶段0摘要列出「待用户指定补读」**，并说明缺这张图时测试点的风险（禁止默默当成无需求）。  
      - 用户回复「补读 imageN / 继续读剩余 P1」后，可在后续回合按同样硬闸继续读，不占“偷跑并行”。
   4. 单张失败/超时：标未解析一次，禁止重试；记入待补读。
   5. 累计已读 4 张仍无阶段0中间稿：先输出中间稿（索引表+已读要点+需求核心初稿+剩余 P0/P1 列表），再继续读。
   6. 仅当用户明确「跳过图片 / 仅基于文字」时才零视觉 Read，并在摘要中**高亮风险：界面/流程类验收可能不全**。

8. **（可选）** MCP `validate-requirement`；未调用不阻断。
9. 文本 + 已排期读图（本回合预算内）结束后，**立即**输出完整阶段0摘要并停下确认。确认项须包含：未读完的 P1 是否要补读、补读优先级。禁止不经确认进阶段1；禁止为读完所有 P2/P3 拖延产出。

### ⛔ 图片读取反模式（禁止）

- ❌ 只读正文、把全部 UI 标成「未读」且不做 P1 缺口判定（会漏测）
- ❌ 按 `image1…imageN` 无差别通读（会卡死）
- ❌ 同一轮并行 Read 多张图，或读完不写要点
- ❌ 无缺口地把「步骤/模版/开证」截图整页升 P0
- ❌ 达到上限后丢弃 P1 且不告诉用户「必须补读哪些」
- ❌ 对已写过要点的图重复 Read

阶段0产物/回复必须包含：

- 输入文件清单。
- **图片分级与读图结果**：总数；P0/P1/P2/P3 列表；已视觉读（路径+要点摘要）；未读 P1（待补读原因/风险）；跳过的 P2/P3；本阶段视觉 Read 张数。
- 需求核心摘要：**正文与已读图要点合并**后的能力说明（主流程、关键界面规则、模板/导入差异等）。
- 流程图/关键界面解析摘要：来自已读 P0/P1；若某主链路仍缺 P1 未读，明确写「主链路图示未闭合，建议补读 …」。
- 需求校验：错误/警告/建议（自检或 MCP）；含「图文不一致」「规则仅在图中」类警告。
- 测试范围待确认项；**默认问一句：是否补读列出的 P1 图片？**

## 阶段1：定位（代码知识库匹配业务流程）

1. **先解析代码知识库路径**（见「代码知识库路径规则」）：
   - 默认查找 `.dev-standards/`；
   - 找不到则提示用户：当前项目没有代码知识库，是否有其他知识库目录？有则请用户告知路径；无则请用户确认是否仅基于需求/接口继续；
   - **在用户回复前不得擅自用活源码替代，也不得跳过询问继续“假装已定位”。**
2. **（可选）** 若 MCP 可用，可调用 `get-business-rule` 辅助列出/匹配业务规则；未调用不阻断。
3. 根据需求内容和（可选）MCP 候选，在知识库 `knowledge/business/` 下匹配对应业务流程文档。
4. 读取匹配到的本地业务文档全文 + 对应 `codemap` 代码地图 + 必要的 `procdefs` 流程定义。
5. 输出到 `.test-standards/<版本>/output/01-定位.md`：
   - 实际使用的知识库根路径。
   - （若使用）MCP 查询关键词、命中文档、排除文档及理由；未使用 MCP 则注明“未使用 MCP”。
   - 受影响流程、关键章节、状态/配置/通知矩阵。
   - 可能涉及的节点/Handler/服务。
   - 需求与哪些既有功能相关。
6. 停下来确认定位是否准确。

### ⛔ 知识库不存在时的强制停止与询问规则

当默认 `.dev-standards/`（及已尝试的常见位置）不存在或为空时，**必须立即停止阶段1**，在对话中明确询问用户，并在 `01-定位.md` 写明原因：

```markdown
## 阶段1定位结果

- 默认代码知识库路径：`.dev-standards/`（未找到或为空）
- MCP：未强制使用；本次未调用 / 或已调用但无有效结果（按实填写）
- 结论：当前项目没有可用的默认代码知识库，已停止。请用户确认：
  1. 是否有其他知识库目录？若有，请提供路径；
  2. 若无，是否仅基于需求文档和 `input/reference/` 接口文档继续进入阶段2（将按知识库缺失降级，回归覆盖可能不完整）。
```

用户回复后：

- **提供了其他知识库路径**：将该路径记为本次知识库根，继续完成阶段1读取与定位。
- **确认无知识库、仅基于需求/接口继续**：在 `01-定位.md` 标注降级，阶段2起主要对照需求与接口文档；阶段3门禁中与 knowledge/codemap 相关项改为“已确认缺失并降级”。
- **用户暂不回复**：保持停止，不得自行读取活源码继续。

**严禁在知识库缺失且用户未确认时做任何替代动作**，包括但不限于：

- ❌ 扫描并读取项目活源码（`.java`、`.xml`、`.yml` 等）来"构建代码地图"
- ❌ 读取 DAO/Service/Application 等实现类来"分析现有逻辑"
- ❌ 把活源码分析结果伪装成 codemap 写入 `01-定位.md`
- ❌ 以"帮助用户了解现状"为由继续读代码

> **原因**：同步开发中，最新代码在开发人员本机，远程仓库中的代码是不完整的早期骨架，不代表最终实现。读取并分析这些代码会得出错误结论，反而误导测试用例设计。

## 阶段2：变更分析（需求 vs 现状）

1. 把需求文档/联动文档描述的目标，与阶段1业务文档的现状逐项对比；若阶段1已确认无知识库，则主要对照需求与接口文档，并标注“无全量业务现状可对”。
2. 如涉及服务/接口边界：优先读本地知识库 `knowledge/service/`；**（可选）** 再调用 MCP `get-service-knowledge`。服务名不确定或未使用 MCP 时，说明依据来源即可，不因未调用 MCP 阻断。
3. 输出到 `.test-standards/<版本>/output/02-变更分析.md`，用表格列变更清单：
   - 新增了什么：新功能/新接口/新枚举值/新状态/新通知。
   - 修改了什么：已有规则/配置/状态迁移的改动。
   - 影响哪些老分支：哪些既有规则、Handler、通知、状态机可能被波及。
   - 三方接口变更点：接口契约、字段、证书/签名逻辑、异常码、超时、幂等。
   - 服务知识补充（来自本地 knowledge/service 和/或可选 MCP）：服务职责、异常处理、幂等、已知风险。
4. 若需求、本地业务文档、（若有）MCP结果互相矛盾，必须单独列「冲突/疑点」并让用户确认。
5. 停下来让用户校正变更清单。

## 阶段3：测试概要（门禁 + 6维度测试点）

> 关键概念：测试点 ≠ 测试用例。测试点是覆盖目标；阶段4再裂变成多条原子用例。本阶段只列测试点，不写详细步骤。

进入阶段3前必须通过门禁：

```markdown
## 阶段3前置门禁

- [ ] 已读取需求文档和接口文档
- [ ] 已按阶段0「正文+缺口驱动读图」处理：已建图片分级（P0–P3）与正文缺口清单；P0/P1 必要图已在硬闸内视觉读并沉淀可测试要点，或未读 P1 已列出待补读风险且用户确认可先降级；无同轮多图并行；未把“读完所有 UI”当作门禁
- [ ] 已读取阶段2确认后的变更清单
- [ ] 已解析代码知识库路径：找到 `.dev-standards` 或用户指定路径；若无，用户已确认仅基于需求/接口降级继续
- [ ] 已读取匹配业务流程文档全文（或已确认知识库缺失并降级）
- [ ] 已读取对应 codemap 代码地图（或已确认缺失并降级）
- [ ] 必要时已读取 procdefs 流程定义（或已确认缺失）
- [ ] MCP 为可选项：已调用则可记录结果；未调用不阻断
- [ ] 已处理需求/业务文档/（若有）MCP结果之间的冲突或疑点
```

门禁不满足时，禁止直接生成 `03-测试概要.md`。注意：**“未调用 MCP / MCP 调不通 / MCP 失败”均不构成门禁失败，不得因此卡死。**

基于确认后的变更清单、业务文档、代码地图和流程图图片解析结果，输出到 `.test-standards/<版本>/output/03-测试概要.md`，按以下 6 个维度列测试点。每个测试点必须标注：覆盖变更项、业务规则来源（本地知识库路径/章节；若用了 MCP 可附加）、接口字段或 Handler 风险点、流程图/图片依据（如有）、预估可裂变用例数。

1. **新功能正向**：新增/修改功能的主流程（测试点可以多条；阶段4里只有其中少数正向主流程标等级1冒烟，其余正向细节标 2）。
2. **配置组合（判定表）**：对照业务文档配置矩阵做组合；维度多时用 pairwise，再补关键业务组合；每个组合用例必须写清每个维度的取值，便于复现。
3. **状态迁移**：对照业务文档状态管理、流程定义和 Handler，覆盖正常迁移 + 异常迁移（退回 back / 拒绝 reject）；反向迁移技巧和预期结果断言规范参照 `reference.md` § 二。
4. **逐步骤功能操作偏离（重点）**：把目标流程拆成有序步骤，并补每个节点前置操作。流程拆分必须同时参考正文、业务文档、流程定义和需求图片中的流程图/时序图/泳道图；若图片展示了正文未写清的分支、角色或节点，要纳入矩阵或列为疑点。识别反向场景时对照 `reference.md` § 五的 **7 类提问**；矩阵只输出前 **6 列**（前置/必填未满足、数量与上限、增删改+重做、操作顺序/路径偏离、重复提交、取消中断重入）。第 7 类取值边界不进矩阵，由阶段4裂变规则展开。只做功能测试，不做安全越权/网络渗透/并发压测。**本阶段每个测试点只需写：场景描述 + 断言方向（如"落库以最后一次操作为准"），不写步骤序列；完整步骤与预期结果在阶段4展开。**
5. **接口与数据边界**：三方接口异常码、字段级空值/格式/枚举/条件必填、报数与开户失败兜底、响应字段映射。
6. **回归影响**：读阶段1代码地图和（若有）本地 service 知识/可选 MCP 服务知识，列出改动涉及 Handler 反查出的必须回归既有功能，重点看风险区域/修改热点。
   - **知识库缺失时的降级处理**：若 codemap 代码地图不存在（无论是否使用 MCP），维度6必须在产物中明确标注："⚠️ 未找到代码知识库/codemap，以下回归点仅基于需求文档描述的改动边界推断，覆盖不完整，建议上线前由开发人员补充受影响模块清单"。不得伪装成完整回归分析，也不得因知识库缺失而直接省略维度6，至少需根据需求文档描述的"修改已有功能"部分推断潜在回归影响范围。

维度4必须输出「步骤 × 操作偏离类别」矩阵：行=流程步骤/前置操作，列=6类（前置/必填未满足、数量与上限、增删改+重做、操作顺序/路径偏离、重复提交、取消中断重入），单元格填反向测试点；不适用标 N/A 并说明原因。**单元格格式为"场景描述 + 断言方向"一句话**，例如：`上传后删除再重传 → 落库以最后一次上传为准（非旧文件）`；禁止在此写出步骤序列，步骤序列和完整断言在阶段4写。

停下来让用户 review，尤其确认逐步骤偏离矩阵有没有漏步骤、漏操作。

### 阶段3结束前必须输出「维度用例预算参考表」

阶段3产物末尾必须附上以下预算表，供阶段4容量评估与漏测预警使用。该表用于识别覆盖盲区，不作为唯一验收门槛；阶段4可基于风险进行合并或裁剪，但必须写明依据与未覆盖风险处置：

```markdown
## 维度用例预算参考表（阶段4容量评估基准）

| 维度 | 来源 | 计算方式 | 下限值 |
|---|---|---|---|
| 维度2-判定表 | 阶段3各判定表 | 所有判定表的有效条件组合行数之和 | ? 条 |
| 维度4-偏离矩阵 | 阶段3各步骤×偏离矩阵 | 所有矩阵中非N/A单元格总数 | ? 条 |
| 维度5-错误码 | 三方接口文档业务错误码 | 有处理逻辑的错误码总数（排除标注"不存在"的） | ? 条 |
| 维度5-请求参数传参逻辑（Dubbo） | 接口文档备注 + 需求文档关键规则 | 关键传参规则条数（固定值/条件必填/映射/默认值/互斥依赖/枚举/透传） | ? 条 |
| 维度5-落库字段 | 三方接口文档响应参数 | 标注"保存"或需落库的响应字段总数 | ? 条 |
| 维度5-通知/邮件 | 需求文档告警/邮件规则 | 通知条数 × 每条的验证要素数（标题/收件人/正文字段/附件） | ? 条 |
| 总计参考量 | — | 阶段3测试点总数 × 2（可按风险调整） | ? 条 |
```

⚠️ 阶段4开始前必须先填写此预算参考表；阶段4结束后需对照说明“哪些覆盖、哪些合并、哪些暂缓及原因”。

## 阶段4：详细用例 + 回归清单

> 原则：一条用例 = 单路径 + 单组数据 + 单个明确预期。不要 1 个测试点只写 1 条用例，也不要一条用例里塞多个校验点。等级1冒烟宜少，**不表示少写用例**：反向、边界、组合仍要裂变，标成 2 或 3。

### 用例裂变规则

- 正反例：正向路径 1 条 + 每一条失败/异常路径各 1 条。
- 等价类：每个关键输入字段，有效值 / 各类无效值 各成一条。
- 边界值：每个关键数值/长度字段，上界、下界、越界各一条（字段长度、金额、日期、数量）。
- 枚举逐值：枚举类输入每个取值至少一条。
- 条件必填：触发条件满足时 1 条 + 不满足时 1 条；不仅限于接口字段，业务逻辑条件必填同样适用（例：法人证件=身份证 → 报四要素；≠ 身份证 → 报二要素）。
- 接口字段级：每字段按必填缺失 / 类型格式错 / 枚举越界 / 条件必填触发拆分；一条只破坏一个字段。
- 多步骤流程：每个独立校验点、每次接口调用、每个 UI 规则各成一条。

**裂变反例与正解**（一条塞多个校验是最常见错误）：

> ❌ 反例：appId空、sign空、custType错、subjectType错 → 预期失败（一条塞了4个校验点）
>
> ✅ 正解：裂成4条，各自单一预期：
> - appId 缺失 → 接口拒绝并提示 appId 必填
> - sign 缺失 → 接口拒绝并提示验签失败
> - custType=PERSON（企业场景）→ 拒绝/类型不符
> - subjectType 与核验对象不符 → 拒绝

**裂变比快速自检**：用例总数低于测试点数 × 2 时需自查是否遗漏裂变；允许基于高风险判断标准对低风险同质场景合并，但须写入合并说明清单；接近 1:1 且无合并说明的，视为可能未裂变，补充分裂或写明合并原因。

**维度专项裂变铁律（不得跳过）**

> **高风险判断标准（以下任一条件满足即为高风险，不得合并）：**
> - 涉及资金计算、金额口径、资金划转或账务落库
> - 接口返回的错误码/状态码有明确的系统处理逻辑（非静默忽略）
> - 需求文档或接口文档中明确标注"必须"、"禁止"、"固定值"、"强制"等字样
> - 状态流转的终态（如成功、失败、余额不足）或不可逆操作
> - 与监管合规、签约协议、KYB/实名认证直接相关的场景
>
> 不满足上述任一条件，且多个场景走同一代码分支、验证同一规则时，可判定为低风险同质场景，允许合并，但必须写入合并说明清单。

- **判定表裂变（维度2）**：阶段3输出的每张判定表，高风险/高频/强监管条件组合应逐行覆盖；低风险且同质组合可按等价类合并，但必须在合并说明清单中写明合并依据与剩余风险。阶段3预算值用于预警，不作为机械下限。
- **偏离矩阵裂变（维度4）**：阶段3中「步骤 × 偏离类别」矩阵应优先覆盖高风险步骤与关键偏离类别；允许对重复、低风险且同质的单元格合并验证，但需在合并说明清单中注明被合并单元格、合并依据和补偿性回归策略。阶段3预算值用于预警，不作为机械下限。

  **各类操作偏离的裂变方法（逐类展开，不得只写一条"操作异常"笼统覆盖）**：

  | 类别 | 裂变方法 | 必带断言 |
  |---|---|---|
  | 1. 前置/必填未满足 | 每个前置操作/必填项未满足各成一条 | 系统阻止推进或给出明确提示；不生成脏数据 |
  | 2. 数量与上限 | 传0个（下限）/ 传最大值 / 传最大值+1（越界）各成一条 | 各边界的系统响应符合规则；不超限通过、超限拦截 |
  | 3. 增删改+重做（**核心**） | 增 / 删 / 改 / 删后重加 / 改后再改 各成一条 | **最终落库内容、数量与最后一次操作一致，不残留旧数据** |
  | 4. 操作顺序/路径偏离 | 每个乱序/跳步/返回重进/切换互斥选项（如线上↔线下）场景各成一条 | 已填数据保留 or 清空符合产品定义；互斥切换后旧数据正确清理 |
  | 5. 重复/多次提交 | 重复提交各成一条 | 不产生重复数据/记录；幂等保护生效 |
  | 6. 取消/中断/重入 | 中断后重入各成一条（草稿续填 / 清空 分别成一条） | 草稿保留 or 清空符合产品定义；重入后数据状态与预期一致 |

  > 类别7（取值边界）不在此矩阵裂变，由通用裂变规则（等价类/边界值/枚举逐值/条件必填）独立展开。
- **接口与数据边界裂变（维度5）**：有三方/Dubbo 时，除响应与错误处理外，重点覆盖请求参数传参逻辑。下面几类**有则展开**；本版本没有对应文档时预算表填 0 并注明，不要编造接口用例：
  1. **错误码逐值**：三方接口文档中每个有处理逻辑的业务错误码（排除标注"不存在"的）各成一条，用例步骤触发该错误码，预期结果写明系统响应行为（自动重试/前端提示/进异常台账/人工介入等）。
  2. **请求参数传参逻辑逐规则**（Dubbo 重点）：接口文档备注或需求文档中定义的关键传参规则（固定值、条件必填、字段映射、默认值、互斥/依赖关系、枚举取值、是否透传）应逐条成用例；每条用例仅校验一条规则，断言实际发起的 Dubbo 请求参数与规则一致。
  3. **响应字段落库逐字段**：接口文档中标注"保存"或需落库的每个响应字段，各成一条用例，断言该字段值正确落库（值来源正确、格式正确、不为空），并且页面相关的响应是正确的。
  4. **业务通知/邮件要素逐条**：需求文档中提及的每条告警邮件/系统通知，必须按「标题、收件人、正文关键字段、附件（如有）」各成一条独立用例；通知类的用例不得以一条"验证邮件发送成功"笼统覆盖。

> **补充检查**：以上4类覆盖了 Dubbo 请求/响应/通知的主链路；同时参照 `reference.md` § 三，对**超时、重试/幂等、签名/证书、联动一致性**（三方返回后本侧多系统状态同步）逐项自查，这4项无专门子类对应但不可遗漏。

### 输出要求

1. 按下方「内置用例模板」写入 `.test-standards/<版本>/output/04-测试用例.md`，执行阶段4时不要再读取外部模板文件。
2. 所有用例必须使用统一表头：`用例等级 | 所属模块 | 用例标题 | 前置条件 | 用例步骤 | 预期结果`。
3. `用例等级` 只允许 `1`、`2`、`3`、`4`（阶段4**默认只用 1/2/3**，低风险不必再单独标 4，归入 3 即可）；`所属模块` 支持按 `/` 分层级，例如 `建档/自主建档/线上授权书`，用于后续转 XMind。

   | 等级 | 含义 | 典型场景 | 条数建议 |
   |---|---|---|---|
   | 1 | **冒烟**：本期新增能力的正向主流程，适合上线前先跑 | 配置/单据保存成功；随后主流程跑通（如启用定时任务后邮件/短信发送成功） | 宜少，一般 **1～3 条**。条数偏多时，多半是把非冒烟场景标进了 1 |
   | 2 | 重要功能与关键规则，每轮回归覆盖 | 配置组合、保存清空、状态/过滤、聚合、隔离、关键通知要素、有处理逻辑的错误码（这些不是冒烟） | 可多于 1，但不是冒烟包 |
   | 3 | 其余 | 字段边界、格式校验、操作偏离细节、次要异常、文案提示、重复提交、同质分隔符 | 默认收口；原先的等级4也放这里 |
   | 4 | 转换脚本仍识别，阶段4一般不用 | — | 建议不用 |

   **等级1怎么标（冒烟）：**
   - 面向**正向、流程性**用例：先完成前置（如新增/保存配置成功），再跑主流程成功。
   - 反向拦截、必填为空、边界值、展示联动分支、清空残留、到期日不命中、聚合拆条、多项目隔离、通知要素拆条、错误码、操作偏离等，**更适合标 2 或 3**，不放进冒烟。
   - 例（国内证贷后到期提醒）：等级1两条即可——① 新增贷后配置保存成功；② 启用定时任务后邮件/短信发送成功。其余关键场景标 **2**，边界/异常/偏离标 **3**。
4. `前置条件` 非必填；如有，转成 XMind 后作为备注放到用例标题节点下。
5. 预期结果必须唯一且可断言：状态断言引用业务文档状态枚举；通知断言引用消息通知场景编码；接口断言写明期望出参/异常码；DB/报数包断言写最终落库字段。
6. 覆盖规则、字段、知识库来源（及若有的 MCP 来源）、Handler 等追溯信息优先融入 `所属模块`、`用例标题`、`用例步骤` 或 `预期结果` 中；不要额外增加表格列，避免影响 XMind 转换。
7. 单独汇总回归清单：改动点 → 影响范围 → 风险等级（分析方法参照 `reference.md` § 六；有 codemap 时做 Handler 反查，无则标注降级推断）。
8. **单独输出「合并说明清单」（禁止跳过）**：凡对判定表行、偏离矩阵单元格、接口字段、错误码、落库字段、通知要素进行了合并或裁剪的，必须在用例表之后另起一节，按以下格式逐条列出，供用户复核：

   ```markdown
   ## 合并说明清单

   | # | 被合并/裁剪项 | 所属维度 | 合并/裁剪原因 | 剩余风险 | 补偿策略 |
   |---|---|---|---|---|---|
   | 1 | 示例：判定表1第3行+第4行 | 维度2 | 两行触发条件相同，仅企业类型不同，系统走同一分支逻辑 | 极低，同一代码路径 | 回归时覆盖其中一行即可 |
   ```

   若本次无任何合并/裁剪，必须明确写"本次无合并/裁剪项"，不得留空或省略该节。

9. **自检（禁止跳过，逐项核查后方可提交）**：

   | # | 自检项 | 检查方法 | 通过标准 |
   |---|---|---|---|
   | ① | 维度2判定表覆盖充分性 | 核对高风险/高频/强监管组合是否逐行覆盖；低风险合并是否有依据 | 高风险组合100%覆盖，合并项可追溯且剩余风险可接受 |
   | ② | 维度4偏离矩阵覆盖充分性 | 核对关键步骤与关键偏离类别覆盖；检查合并单元格说明 | 关键步骤与关键偏离类别100%覆盖，合并说明完整 |
   | ③ | 维度5-错误码覆盖 | 列出接口文档所有有效业务错误码，逐一确认已各成独立用例 | 所有有效错误码均有对应用例 |
   | ④ | 维度5-请求参数传参逻辑覆盖（Dubbo） | 列出接口文档备注/需求文档中的关键传参规则（固定值、条件必填、映射、默认值、互斥/依赖等），逐一确认对应校验用例 | 关键传参规则100%有对应用例，且每条规则可追溯到文档来源 |
   | ⑤ | 维度5-落库字段覆盖 | 列出接口文档标注"保存"的响应字段，逐一确认已各成落库验证用例 | 所有需落库字段均有对应用例 |
   | ⑥ | 维度5-通知/邮件要素覆盖 | 列出需求文档中所有通知/邮件场景，确认已按要素（标题/收件人/正文字段/附件）各成用例 | 无通知场景被笼统覆盖 |
   | ⑦ | 总体覆盖与成本平衡 | 对照阶段3预算参考量，评估是否存在明显漏测；说明裁剪/合并原因 | 不存在关键漏测，覆盖结论可被复核，成本与收益匹配 |
   | ⑧ | 等级1是否符合冒烟 | 列出全部等级1标题，看是否都是本期新增正向主流程、条数是否精简 | 等级1宜少、走正向主流程；关键非冒烟场景标 2，其余标 3 |

   结果处置按风险分级：高风险项（核心链路/资金口径/强监管/生产高频）未通过时建议补齐后再输出；中低风险项未通过时，可在产物中登记「未覆盖项、原因、影响评估、补偿性回归计划、预计补齐时间」，并经用户确认后继续输出。

10. **阶段4收尾必须询问是否转 XMind**（见下文「转 XMind」）：写出 `04-测试用例.md` 并完成自检后停下询问，用户同意相关话术后才转换；禁止本阶段自动转。

### 内置用例模板

阶段4生成 `04-测试用例.md` 时，直接使用以下模板：

```markdown
# 测试用例 - <版本>

> 需求来源：`.test-standards/<版本>/input/prodword/`  
> 参考资料：`.test-standards/<版本>/input/reference/`  
> 输出位置：`.test-standards/<版本>/output/04-测试用例.md`

## 表格规范

必填字段：

- `用例等级`：默认标 `1`（冒烟，少量正向主流程）、`2`（重要/关键规则）、`3`（其余）。一般不用 `4`。
- `所属模块`：支持按 `/` 分层级，后续转 XMind 使用。例如 `建档/自主建档/线上授权书`。
- `用例标题`：单条原子用例标题，标题应能独立表达校验目标。
- `用例步骤`：单路径、单组数据、单个明确动作链；多步用 `<br>` 或 `1. 2. 3.` 分隔。
- `预期结果`：唯一明确预期，必须可断言。同一路径若有多条断言，用中文分号 `；` 分隔，供转 XMind 拆成编号子节点。

非必填字段：

- `前置条件`：如有，转成 XMind 后作为备注放到用例标题节点下。

## 用例表

| 用例等级 | 所属模块 | 用例标题 | 前置条件 | 用例步骤 | 预期结果 |
|---|---|---|---|---|---|

不要把「示例：…」样例行写入产物表（转换脚本会跳过标题以 `示例：` 开头的行，但仍不要依赖这一兜底）。

## 填写要求

- 一条用例只覆盖一个明确校验点，禁止一条用例塞多个异常字段或多个分支。
- `用例等级`：1 给冒烟正向主流程（宜少，一般 1～3 条）；非冒烟的关键场景标 2；其余标 3。拦截/边界/分支不放进等级1。
- `所属模块` 必须能形成 XMind 层级，不要为空。
- `用例标题` 中不要手写等级；转 XMind 时用优先级图标表示等级，不要把 `【等级N】` 写进标题。
- `用例步骤` 内如需换行，优先使用 `<br>`；转 XMind 后全部步骤会合并为一个多行节点。
- `预期结果` 多条断言用中文分号 `；` 分隔，转 XMind 后会拆成 `1、xxx`、`2、yyy`。
```

### 转 XMind（阶段4完成后询问，禁止自动转）

阶段4写出 `.test-standards/<版本>/output/04-测试用例.md` 并完成自检后，**必须停下来询问用户**，**禁止**在同一回合自动转 XMind。

固定询问话术（可微调，但必须明确征求同意）：

> 阶段4测试用例已生成：`.test-standards/<版本>/output/04-测试用例.md`  
> 是否转换为 XMind（`.test-standards/<版本>/output/<版本>.xmind`）？同意的话直接回复「转 XMind」或「同意」。

**仅当用户明确同意后再调用脚本。** 同意类话术包括（不区分大小写）：同意、好、要、可以、转、转一下、转 XMind、转xmind、转成 xmind、转脑图、生成 xmind、md to xmind、转换成脑图。用户只说「继续」而未提转换时，不要转，再确认一句是否要 XMind。

拒绝或不转：不需要、不用、先不转、下次再说、只要 md。记录「用户暂不转 XMind」后结束，不要调用脚本。

用户同意后必须走内置脚本（含中文版本名优先 `--version-prefix`）：

```bash
python "<本skill目录>/scripts/convert_cases_to_xmind_utf8.py" "<项目根目录>" --version-prefix "V1.6.1"
```

默认：

- 读：`.test-standards/<版本>/output/04-测试用例.md`
- 写：`.test-standards/<版本>/output/<版本>.xmind`

阶段4尚未产出 md 时禁止转 XMind。用户在后续单独说「把该版本转 XMind」且 md 已存在时，视为同意，可直接调用上述脚本。

表头、字段规则、XMind 层级与图标映射见 `reference.md` § 七。转换脚本只做格式转换，不改业务语义；表头不符时先改 md 再转，禁止自写 XMind 转换器。

## 注意事项

- 全程中文。术语与业务文档保持一致。
- 知识库与代码只读，不修改 `.dev-standards`（或用户指定的知识库目录）和业务源码。
- **不强制 MCP；MCP 调不通必须 fail-open，禁止重试卡死。** 若使用了 MCP，其结果只是辅助证据，不是最终真相；须和本地文档、代码地图、用户确认结论交叉校验。
- 若业务文档、需求、（若有）MCP结果出现矛盾，明确指出并提示用户确认。
- 阶段产物要写清楚来源，避免“AI拍脑袋”的测试点。
- **阶段0 转换只用内置脚本**（`convert_version_inputs_utf8.py` / `convert_to_md.py`）。禁止自写 Word/Excel 转换脚本，禁止 mammoth 一行命令（会把表格展平），禁止把临时 `.py` / 版本名文件写进项目。
- **阶段4 转 XMind 必须先问再转。** 写出 `04-测试用例.md` 后询问；用户同意相关话术后才调用 `convert_cases_to_xmind_utf8.py`。禁止阶段4结束时自动转；已无独立 `tc-convert` skill。
- 需求文档图片可能含流程图或大量 UI 截图。阶段0必须抽取到 `prodword_pic/`，但**文本优先**：先读 md/reference 建图片索引与缺口清单。P0 流程专图、正文写不清的 P1（含关键 UI）才视觉读；P2/P3 默认不读。硬闸与阶段0正文一致：**每轮最多 1 张图、读后先写可见要点、阶段0累计视觉读默认上限 8 张**。禁止按 image1…N 连环读界面图。阶段3用已沉淀文字摘要，不要为过门禁再通读目录。
- **代码知识库默认路径为 `.dev-standards`。** 找不到时必须询问用户是否有其他知识库目录；有则按用户路径继续，无则在用户确认后按需求/接口降级继续。
- **不得以活源码替代任何知识库文档**。`codemap`、`business`、`procdefs` 均指代码知识库下的**静态文档**。当这些文档不存在且用户尚未确认处理方式时，必须停下来询问，绝不自行读取 `.java`/`.xml`/`.yml` 等活源码作为替代。
